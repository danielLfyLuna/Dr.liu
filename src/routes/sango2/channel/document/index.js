// import cells from './cells'
// import groups from './groups'

export default (store) => ({
  path: 'document',
  breadcrumbName: '说明文档',
  childRoutes: [
    // cells(store),
    // groups(store)
  ]
})
