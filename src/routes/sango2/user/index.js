// import cells from './cells'
// import groups from './groups'

export default (store) => ({
  path: 'user',
  breadcrumbName: '权限管理',
  childRoutes: [
    // cells(store),
    // groups(store)
  ]
})
